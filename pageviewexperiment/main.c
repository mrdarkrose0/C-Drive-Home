#include <stdio.h>
#include <stdlib.h>

int main()
{
    int pageview = 0;
    pageview = pageview + 1;
    printf("Page view is: %d", pageview);
    pageview = pageview + 1;
    printf("\nPage view is: %d", pageview);
    pageview = pageview + 1;
    printf("\nPage view is: %d", pageview);
    return 0;
}
